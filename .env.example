# Discord
DISCORD_TOKEN=your_discord_bot_token_here

# Google OAuth
GOOGLE_CREDENTIALS_FILE=credentials.json  # Path to your OAuth client credentials
GOOGLE_TOKEN_DIR=data/tokens              # Where to store user tokens

# Bot Settings
NOTIFICATION_CHANNEL_ID=0                 # Channel ID for meeting notifications (0 = DM only)
POLL_INTERVAL_SECONDS=60                  # How often to check for new calendar events
TIMEZONE=UTC                              # Default timezone for display
